# Environment files with secrets
.env
.env.local
.env.*.local

# Local persistent data (bind mounts)
data/

# Keep the example
!.env.example

# Session notes (contain personal data and secrets)
MEMORIA_SESSAO.md

# IDE / editor settings
.claude/

# Windows artifacts
nul

# CLIProxyAPI (downloaded binaries and local config)
CLIProxyAPI/

# Windows artifacts from old setup
%USERPROFILE%/
